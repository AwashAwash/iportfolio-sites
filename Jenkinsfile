
pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                echo 'Jenkins is alive!'
                sh 'date'
                sh 'whoami'
                sh 'pwd'
                sh 'ls -la'
            }
        }
    }
}
